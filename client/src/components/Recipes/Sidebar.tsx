import SidebarExpanded from "./SidebarExpanded";

const Sidebar = () => {

  return (
    <aside className={`relative w-48 whitespace-nowrap`}>
      <SidebarExpanded />
    </aside>
  );
};

export default Sidebar;
